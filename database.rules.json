{
  "rules": {
    ".read": false,
    ".write": false,
    "userHouseholds": {
      "$uid": {
        ".read": "auth != null && auth.uid == $uid",
        ".write": "auth != null && auth.uid == $uid",
        ".validate": "newData.isString() && newData.val().length < 128"
      }
    },
    "invites": {
      "$token": {
        ".read": "auth != null",
        ".write": "auth != null && ((!data.exists() && newData.exists() && newData.child('createdBy').val() == auth.uid && root.child('households/' + newData.child('hid').val() + '/members/' + auth.uid).exists()) || (data.exists() && !newData.exists() && root.child('households/' + data.child('hid').val() + '/members/' + auth.uid).exists()))",
        ".validate": "newData.child('hid').isString() && newData.child('expiresAt').isNumber()",
        "usedBy": {
          ".write": "auth != null && !data.exists() && newData.val() == auth.uid"
        }
      }
    },
    "households": {
      "$hid": {
        "members": {
          ".read": "auth != null && data.child(auth.uid).exists()",
          "$uid": {
            ".write": "auth != null && ((newData.exists() && auth.uid == $uid && ($hid == auth.uid || (newData.child('inviteToken').isString() && root.child('invites/' + newData.child('inviteToken').val() + '/hid').val() == $hid && root.child('invites/' + newData.child('inviteToken').val() + '/expiresAt').val() > now && !root.child('invites/' + newData.child('inviteToken').val() + '/usedBy').exists()))) || (!newData.exists() && (auth.uid == $uid || root.child('households/' + $hid + '/meta/ownerUid').val() == auth.uid)))",
            ".validate": "(newData.isString() && newData.val().length < 256) || (newData.child('email').isString() && newData.child('email').val().length < 256)"
          }
        },
        "state": {
          ".read": "auth != null && root.child('households/' + $hid + '/members/' + auth.uid).exists()",
          ".write": "auth != null && root.child('households/' + $hid + '/members/' + auth.uid).exists()"
        },
        "stateV1Backup": {
          ".read": "auth != null && root.child('households/' + $hid + '/members/' + auth.uid).exists()",
          ".write": "auth != null && root.child('households/' + $hid + '/members/' + auth.uid).exists()"
        },
        "meta": {
          ".read": "auth != null && root.child('households/' + $hid + '/members/' + auth.uid).exists()",
          ".write": "auth != null && root.child('households/' + $hid + '/members/' + auth.uid).exists()",
          "schemaVersion": {
            ".validate": "newData.isNumber()"
          }
        },
        "settings": {
          ".read": "auth != null && root.child('households/' + $hid + '/members/' + auth.uid).exists()",
          ".write": "auth != null && root.child('households/' + $hid + '/members/' + auth.uid).exists()",
          "hourlyRateCents": { ".validate": "newData.isNumber() && newData.val() >= 0" },
          "fuelRateCents": { ".validate": "newData.isNumber() && newData.val() >= 0" }
        },
        "week": {
          ".read": "auth != null && root.child('households/' + $hid + '/members/' + auth.uid).exists()",
          ".write": "auth != null && root.child('households/' + $hid + '/members/' + auth.uid).exists()",
          "weekStart": {
            ".validate": "newData.isString() && newData.val().matches(/^[0-9]{4}-[0-9]{2}-[0-9]{2}$/)"
          },
          "bonusCents": { ".validate": "newData.isNumber() && newData.val() >= 0" },
          "carryoverCents": { ".validate": "newData.isNumber() && newData.val() >= 0" }
        },
        "cash": {
          ".read": "auth != null && root.child('households/' + $hid + '/members/' + auth.uid).exists()",
          ".write": "auth != null && root.child('households/' + $hid + '/members/' + auth.uid).exists()",
          "counts": {
            "$bill": { ".validate": "newData.isNumber() && newData.val() >= 0" }
          }
        },
        "cashTransactions": {
          ".read": "auth != null && root.child('households/' + $hid + '/members/' + auth.uid).exists()",
          ".write": "auth != null && root.child('households/' + $hid + '/members/' + auth.uid).exists()",
          "$id": {
            "amountCents": { ".validate": "newData.isNumber() && newData.val() >= 0" },
            "label": { ".validate": "newData.isString() && newData.val().length < 512" }
          }
        },
        "history": {
          ".read": "auth != null && root.child('households/' + $hid + '/members/' + auth.uid).exists()",
          ".write": "auth != null && root.child('households/' + $hid + '/members/' + auth.uid).exists()",
          "$id": {
            "amountPaidCents": { ".validate": "newData.isNumber() && newData.val() >= 0" },
            "totalCents": { ".validate": "newData.isNumber()" }
          }
        },
        "archivedWeeks": {
          ".read": "auth != null && root.child('households/' + $hid + '/members/' + auth.uid).exists()",
          ".write": "auth != null && root.child('households/' + $hid + '/members/' + auth.uid).exists()"
        },
        "priorPayments": {
          ".read": "auth != null && root.child('households/' + $hid + '/members/' + auth.uid).exists()",
          ".write": "auth != null && root.child('households/' + $hid + '/members/' + auth.uid).exists()",
          "$id": {
            "amountCents": { ".validate": "newData.isNumber() && newData.val() >= 0" },
            "dateKey": { ".validate": "newData.isString() && newData.val().matches(/^[0-9]{4}-[0-9]{2}-[0-9]{2}$/)" }
          }
        }
      }
    }
  }
}
